{
  "name": "yj-monorepo",
  "version": "1.0.0",
  "description": "A test monorepo for Mastra projects",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm --filter weather-agent dev",
    "build": "echo \"Building packages...\" && pnpm --filter shared-utils build && pnpm --filter weather-agent build && echo \"Build completed successfully!\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
